<mah:MetroWindow x:Class="rpkg.MessageQuestion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:rpkg"
        mc:Ignorable="d"
        Loaded="Window_Loaded" Title="" Height="150" Width="300" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.ColumnSpan="2" x:Name="message" Content="" FontSize="14" Margin="10"/>
        <Button Grid.Row="2" Grid.Column="1" Click="OKButton_Click" x:Name="OKButton" Content="OK" />
        <Button Grid.Row="2" Grid.Column="3" Click="CancelButton_Click" x:Name="CancelButton" Content="Cancel" />
    </Grid>
</mah:MetroWindow>
